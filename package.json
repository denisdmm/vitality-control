{
	"name": "vitality-control",
	"private": true,
	"version": "0.1.0",
	"description": "Central de Vitalidade — monorepo (NestJS backend + Angular frontend)",
	"workspaces": [
		"backend",
		"frontend"
	],
	"scripts": {
		"tunnel": "node backend/tunnel.js",
		"start": "npx --yes concurrently -n backend,frontend,tunnel -c blue,green,yellow \"npm run start:dev -w backend\" \"npm run start -w frontend\" \"npm run tunnel\"",
		"start:dev": "npm run start:dev -w backend",
		"start:frontend": "npm run start -w frontend",
		"build": "npm run build -w backend && npm run build -w frontend",
		"build:frontend": "npm run build -w frontend",
		"build:backend": "npm run build -w backend",
		"test": "npm run test -w frontend",
		"prisma:deploy": "npm run prisma:deploy -w backend",
		"prisma:seed": "npm run prisma:seed -w backend"
	},
	"engines": {
		"node": ">=20"
	}
}